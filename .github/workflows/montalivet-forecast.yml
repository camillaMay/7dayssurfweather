name: Fetch Surf Forecast HTML

permissions:
  contents: write

on:
  schedule:
    # Runs once a day at 06:00 UTC (adjust as needed)
    - cron: '0 6 * * *'
  workflow_dispatch: {}

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Fetch raw HTML (hourly page)
        run: |
          curl -sL \
            -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" \
            "https://www.surf-forecast.com/breaks/Montalivetles-Bains/forecasts/latest" \
            -o data/montalivet-hourly.html

      - name: Fetch raw HTML (16-day page)
        run: |
          curl -sL \
            -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" \
            "https://www.surf-forecast.com/breaks/Montalivetles-Bains/forecasts/latest/six_day" \
            -o data/montalivet-16day.html

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add montalivet-hourly.html montalivet-16day.html
          git diff --quiet --cached || git commit -m "Update surf forecast HTML $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          git push
